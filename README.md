# To add a new project going forward

1. Duplicate the whole _template/ folder, rename it to new project's slug
2. Fill in the placeholders in its myproject.html — the README inside that folder walks through exactly what to replace
3. Add a project card on the homepage pointing to it
4. Drop screenshots in that project's own images/ folder, docs in docs/
